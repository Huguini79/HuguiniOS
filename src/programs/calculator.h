#ifndef CALCULATOR_H

#include <stdbool.h>

extern bool app_calculadora;

void ensenar();

int sumar(int n, int n2);
int restar(int n, int n2);
int multiplicar(int n, int n2);
int dividir(int n, int n2);

#endif
